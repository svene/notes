# Java Tips and Tricks

## Parsing for Input Validation

(Code taken from https://github.com/svene/2026-07-26_quarkus-hexagon-demo-mm-per-tech)

````java
// e.g. Kafka Listener:
public class FruitDeliveryReceiver {

  @Incoming("fruit-deliveries")
  @Blocking
  public void receive(RawFruitDelivery message) {
    // Mapping: RawFruitDelivery -> FruitDelivery:
    Optional<FruitDelivery> x = FruitDelivery.parse(message.productName(), message.quantity());
    // Validation:
    if (x.isEmpty()) return; // TODO: validation error reporting
    // Processing:
    IO.printlin("product: ", x.get().productName() + " qty=" + x.get().quantity());
  }
  
}
````

````java
public record RawFruitDelivery(String productName, int quantity) {}
````

````java
public record FruitDelivery(String productName, int quantity) {

  private static final int MAX_QUANTITY = 10_000;

  private static boolean isValid(int quantity) {
    return quantity > 0 && quantity <= MAX_QUANTITY;
  }

  public static Optional<FruitDelivery> parse(String productName, int quantity) {
    return isValid(quantity)
      ? Optional.of(new FruitDelivery(productName, quantity))
      : Optional.empty();
  }
}
````

### Open Items

- compare with e.g. SpringBoots built-in validation (e.g. RestControllers)
- compare with @Valid, @Validated

### References

1. https://github.com/svene/2026-07-26_quarkus-hexagon-demo-mm-per-tech
1. Medium Articles: TODO


## Product and Sum Types

### References

1. Medium Articles: TODO

## @JsonView (Jackson) DTOs

### References

1. Medium Articles: TODO
